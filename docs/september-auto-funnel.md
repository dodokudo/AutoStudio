# 9月オート・ファネル分析（2026-09-11 ローカル検証）

## 対象と変更箇所

- 画面: `/line` → ファネル分析 → 9月ファネル。カスタムファネルの作成ではない。
- `src/lib/lstep/septemberAutoColumns.ts`: 9月オートのタグ19項目と友だち情報1項目をCSVの内部IDで識別。
- `src/lib/lstep/rawCsvLoader.ts`: IDからBigQuery列へ変換し、タグはINTEGER、申込枠はSTRINGに固定。引用符・改行入りCSVを扱う。
- `src/app/api/line/panel-analysis/route.ts`: 9月はオート用タグ・申込日時を使用。概要、日別、流入別、属性別、枠別、リードタイムを切り替える。
- `src/app/(autostudio)/line/_components/PanelAnalysis.tsx`: 9月のタグ別人数と10時・13時・20時枠を表示。流入経路別はThreadsのポスト・プロフ・固定を初期表示から常時表示し、開閉ボタンは置かない。各内訳に登録・回答・申込・参加・購入・ブロックを表示。ファネル図は「セミナー参加 → 商品LPタップ → 購入」の順で、LPタップ率は参加者、購入への移行率はLPタップ者を分母にする。登録日別の進捗にも参加と購入の間にLPタップ人数・LPタップ率を表示し、購入率の分母もLPタップ者に揃える。内訳も選択期間に連動。複数経路の印がある人は重複加算せず「複数経路」、内訳なしは「内訳不明」に計上。未取得項目は数値と比率を「—」にする。
- `src/lib/lstep/funnel-campaigns.ts`: ユーザーの最終指定により、9月の初期対象は2026-09-08以降の新規登録者。右側の共通期間選択で指定した場合はその期間を優先し、登録日で全集計を絞り込む。回答完了は共通タグ「アンケート：回答完了」（タグ_8087272）で判定。
- `src/lib/lstep/funnel.ts`: タグ選択肢に9月オートの表示名を追加。

## 取り込み・検証結果

提供CSV `2026.09 エバーのコピー_20260911184835.csv` の2,773人を、BigQuery `mark-454114.autostudio_lstep` に取り込み済み。

| テーブル | 2026-09-11の行数 |
|---|---:|
| lstep_friends_raw | 2,773 |
| user_core | 2,773 |
| user_tags | 385,447 |
| user_sources | 27,730 |
| user_surveys | 5,546 |
| user_info | 3,024 |

取込前のraw表は `lstep_friends_raw_before_20260911_auto` に保存済み（2,772人）。

初回検証は9/8以降・オート専用回答タグで対象50人、回答35人だったが、これは回答完了の判定として不適切だった。最終修正後は9/8以降の登録50人、共通回答タグ45人で90.0%。カスタムで9/7〜9/30を指定すると58人、53人で91.4%。9/7〜9/10に限ると53/57人で93.0%。9月オート回答済みタグの35人はタグ別診断にのみ残す。CSV・BigQuery・ローカルAPI・画面で照合済み。参加タグは視聴リンクのタップであり、録画視聴完了を意味しない。

7月は取込前後で対象351人、回答305人、動画115人、申込103人、参加78人、購入4人が一致。

検証: CSV変換・キャンペーンのテスト7件、型チェック、ビルド、7月/9月API、ブラウザの月切替と枠別表示。

## 継続運用の残作業

1. CSVに期待感50%・75%・100%・125%の4タグが含まれていない。LSTEP定期出力項目に `タグ_10472543`、`タグ_10472550`、`タグ_10472553`、`タグ_10472557` を追加する必要がある。期待感アンケート回答総数は取得済み（2人）。
2. 日次取得は `src/lib/lstep/downloader.ts` でお気に入りの一番上を利用する。9月の出力設定がそこで選ばれることを実際の定期実行で確認する必要がある。
3. 変更コードはローカルのみ。本番ダッシュボードおよびCloud Run `autostudio-lstep` への反映は未実施。旧ジョブがraw表を上書きするため、継続運用完了とは扱わない。ジョブの既存ビルド定義は `deploy/lstep/cloudbuild.yaml`。
4. 回答のリードタイムは友だち情報「アンケート回答日」（友だち情報_2685543）から算出。9/8以降は当日44人・翌日1人。回答完了はuser_surveysに分類されるためuser_tagsの検索では取得できない。申込・参加・購入はタグを日次CSVで初めて確認した日による推定であり、初回取得以前の実際の操作日は分からない。

ローカル確認先: http://localhost:3001/line → ファネル分析 → 9月ファネル。
